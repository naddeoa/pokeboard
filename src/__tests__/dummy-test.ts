


describe('something', () => {

    it('to work', () => {
        // These are the methods that you can use for expect
        // https://jestjs.io/docs/en/expect
        expect(true).toBe(true)
    })

    it('to work this way too', () => {
        expect(true).not.toBe(false)
    })

})

